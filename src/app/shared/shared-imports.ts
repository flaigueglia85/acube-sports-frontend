import { CommonModule } from '@angular/common';
import { RouterModule } from '@angular/router';
import { NgIconComponent } from '@ng-icons/core';

export const SHARED_IMPORTS = [CommonModule, RouterModule, NgIconComponent];
